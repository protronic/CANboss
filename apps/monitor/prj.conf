# Zephyr-Konfiguration des CANboss-Monitors

CONFIG_CAN=y

# UI + SDO-Transfers laufen im Main-Thread; CAN-RX und CANopen-Mainline
# sind eigene k_threads (src/osal_zephyr.c, statische Stacks).
CONFIG_MAIN_STACK_SIZE=16384

# Kein Boot-Banner in die TUI drucken
CONFIG_BOOT_BANNER=n
