/*
 * native_sim: CAN ueber das Host-SocketCAN-Interface anbinden
 * (wie apps/monitor; vcan0 anlegen: sudo ip link add dev vcan0 type vcan
 *  && sudo ip link set vcan0 up).
 */

/ {
	chosen {
		zephyr,canbus = &can0;
	};
};

&can0 {
	status = "okay";
	host-interface = "vcan0";
};
