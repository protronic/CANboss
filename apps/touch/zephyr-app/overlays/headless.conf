# Headless-Variante (CI/Container ohne libsdl2): Dummy-Display statt
# SDL-Fenster; die LVGL-UI laeuft vollstaendig, nur unsichtbar.
CONFIG_SDL_DISPLAY=n
CONFIG_INPUT_SDL_TOUCH=n
CONFIG_DUMMY_DISPLAY=y
# Debug-Werkzeuge im Headless-Betrieb
CONFIG_CAN_SHELL=y
CONFIG_DEVICE_SHELL=y
